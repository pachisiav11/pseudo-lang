error[E3080]: `Small` is ARRAY[1:3] OF INTEGER, but this is ARRAY[1:4] OF INTEGER
 --> program.pseudo:3:1
  |
3 | Small <- Large
  | ^^^^^^^^^^^^^^ a different shape
  |
help: Assigning a whole array needs the same element type and the
      same bounds on both sides.
